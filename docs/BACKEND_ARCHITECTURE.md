# KodumunHatasi Backend Mimarisi

> İstek akışı, veri işleme ve AI entegrasyonunun detaylı açıklaması

---

## 🏗️ Genel Mimari

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              CLIENT (Frontend)                          │
└───────────────────────────────────┬─────────────────────────────────────┘
                                    │ HTTP Request
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              EXPRESS SERVER                              │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────┐  │
│  │   Middleware    │  │     Routes      │  │      Controllers        │  │
│  │  (auth, cors)   │──│  (path match)   │──│  (request handling)     │  │
│  └─────────────────┘  └─────────────────┘  └───────────┬─────────────┘  │
└───────────────────────────────────────────────────────┼─────────────────┘
                                                        │
                                    ┌───────────────────┴──────────────────┐
                                    ▼                                      ▼
                    ┌────────────────────────────┐         ┌───────────────────────────┐
                    │         SERVICES           │         │        EXTERNAL APIs      │
                    │  (Business Logic Layer)    │◀───────▶│  • Gemini AI (Analysis)   │
                    └──────────────┬─────────────┘         │  • Supabase Auth          │
                                   │                       └───────────────────────────┘
                                   ▼
                    ┌────────────────────────────┐
                    │       PRISMA CLIENT        │
                    │    (Database Abstraction)  │
                    └──────────────┬─────────────┘
                                   │
                                   ▼
                    ┌────────────────────────────┐
                    │         SUPABASE           │
                    │    (PostgreSQL Database)   │
                    └────────────────────────────┘
```

---

## 📁 Katman Yapısı

| Katman          | Dosyalar           | Sorumluluk                                       |
| --------------- | ------------------ | ------------------------------------------------ |
| **Routes**      | `routes/*.ts`      | URL path matching, middleware bağlama            |
| **Controllers** | `controllers/*.ts` | Request parsing, validation, response formatting |
| **Services**    | `services/*.ts`    | İş mantığı, veri işleme, AI çağrıları            |
| **Middleware**  | `middleware/*.ts`  | Authentication, logging, error handling          |
| **Utils**       | `utils/*.ts`       | Prisma client, Supabase client                   |

---

## 🔐 Authentication Flow

```
┌──────────┐       ┌────────────────┐       ┌───────────────┐       ┌────────────┐
│  Client  │──────▶│  Auth Header   │──────▶│   Middleware  │──────▶│  Controller │
│          │       │  Bearer <JWT>  │       │   auth.ts     │       │            │
└──────────┘       └────────────────┘       └───────┬───────┘       └────────────┘
                                                    │
                                    ┌───────────────┴───────────────┐
                                    ▼                               ▼
                            [Token Valid]                    [Token Invalid]
                                    │                               │
                                    ▼                               ▼
                           req.userId = sub              401 Unauthorized
                                    │                    {success: false,
                                    │                     error: "..."}
                                    ▼
                              next() ─────▶ Controller'a devam
```

### auth.ts Middleware Detayı

```typescript
// 1. Header kontrolü
if (!authHeader || !authHeader.startsWith("Bearer ")) {
  return 401: "Missing or invalid authorization header"
}

// 2. JWT decode (base64 payload çözme)
const payload = decodeJwtPayload(token);
// payload = { sub: "user-uuid", email: "...", exp: ... }

// 3. User ID'yi request'e ekle
req.userId = payload.sub;

// 4. Controller'a devam
next();
```

> ⚠️ **Not:** Şu an JWT sadece decode ediliyor, cryptographic verification yok. Production için `supabase.auth.getUser(token)` kullanılmalı.

---

## 📤 Kod Yükleme ve Analiz Akışı

Bu sistemin en kritik akışı. Kullanıcı kod yüklediğinde neler oluyor:

```
┌──────────────┐
│ POST /api/   │
│ versions/    │
│ project/:id  │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                        codeVersionController.uploadVersion               │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 1. projectService.findById(projectId, userId)                      │  │
│  │    → Proje var mı? Kullanıcıya mı ait? (ownership check)           │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 2. codeVersionService.create(projectId, versionLabel)              │  │
│  │    → Database'e yeni CodeVersion kaydı oluştur                     │  │
│  │    ⚠️ sourceCode SAKLANMIYOR - sadece versionLabel                 │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 3. analysisService.analyzeCode(version.id, sourceCode)             │  │
│  │    → AI'a gönder, sonucu database'e kaydet                         │  │
│  │    → sourceCode AI'a gidiyor ama KAYDEDILMIYOR                     │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 4. codeVersionService.findById(version.id)                         │  │
│  │    → Yeni oluşturulan version + analysis birlikte getir            │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│                            Response 201 Created                          │
│                            { success: true, data: {...} }                │
└──────────────────────────────────────────────────────────────────────────┘
```

### analysisService.analyzeCode() Detayı

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         analysisService.analyzeCode                      │
│                                                                          │
│   Input: codeVersionId, sourceCode                                       │
│                                                                          │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │                    STEP 1: GEMINI AI CALL                        │   │
│   │                                                                  │   │
│   │   Prompt içeriği:                                                │   │
│   │   - Strict JSON format şeması                                    │   │
│   │   - Allowed issueCode'lar: NESTED_LOOP, UNUSED_VARIABLE,         │   │
│   │     MAGIC_NUMBER, LONG_FUNCTION, DUPLICATE_CODE                  │   │
│   │   - Severity: low, medium, high                                  │   │
│   │   - Complexity: O_1, O_n, O_n2                                   │   │
│   │                                                                  │   │
│   │   AI Response (parsed JSON):                                     │   │
│   │   {                                                              │   │
│   │     "summary": "...",                                            │   │
│   │     "issues": [{ issueCode, severity, complexity, ... }]         │   │
│   │   }                                                              │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │                    STEP 2: DATABASE PERSIST                      │   │
│   │                                                                  │   │
│   │   prisma.analysisResult.create({                                 │   │
│   │     codeVersionId,                                               │   │
│   │     summary: aiResponse.summary,                                 │   │
│   │     issues: {                                                    │   │
│   │       create: aiResponse.issues.map(issue => ({                  │   │
│   │         issueCode,                                               │   │
│   │         severity,                                                │   │
│   │         complexity,                                              │   │
│   │         functionName?,                                           │   │
│   │         startLine?, endLine?,                                    │   │
│   │         snippet: { create: { before, after } }                   │   │
│   │       }))                                                        │   │
│   │     }                                                            │   │
│   │   })                                                             │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│   Output: Kaydedilen AnalysisResult (issues dahil)                       │
└─────────────────────────────────────────────────────────────────────────┘
```

### Veri Akış Diyagramı

```
sourceCode (string)
       │
       ├──────────────────────────────────────────┐
       ▼                                          │
   ╔═══════════════════════════════╗              │
   ║        GEMINI AI              ║              │
   ║   (External API Call)         ║              │
   ║                               ║              │
   ║   "Analiz et ve JSON döndür"  ║              │
   ╚═══════════════╦═══════════════╝              │
                   │                              │
                   ▼                              │
        { summary, issues[] }                     │
                   │                              │
                   ▼                              │
         ╔═════════════════════════════╗          X
         ║      POSTGRESQL DB          ║          │
         ║                             ║          │
         ║  AnalysisResult             ║   sourceCode
         ║    └── AnalysisIssue[]      ║   SAKLANMIYOR!
         ║          └── Snippet?       ║          │
         ╚═════════════════════════════╝          │
                                                  │
                               Garbage Collection ◀──┘
```

---

## 🔄 Karşılaştırma (Comparison) Akışı

**KRİTİK:** Karşılaştırma tamamen **DETERMİNİSTİK**'tir. AI burada **KULLANILMAZ**.

```
┌──────────────────┐
│ POST /api/       │
│ comparisons/     │
│ project/:id      │
│                  │
│ Body:            │
│ {fromVersionId,  │
│  toVersionId}    │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    comparisonController.compareVersions                  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 1. Ownership & Validation                                          │  │
│  │    - projectService.findById() → Proje kullanıcıya ait mi?         │  │
│  │    - codeVersionService.findById(from) → Proje'ye ait mi?          │  │
│  │    - codeVersionService.findById(to) → Proje'ye ait mi?            │  │
│  │    - analysisService.getByCodeVersionId() → İki versiyon da        │  │
│  │      analiz edilmiş mi?                                            │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 2. comparisonService.compareAnalyses()                             │  │
│  │    → Deterministik karşılaştırma mantığı (aşağıda detaylı)         │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│                            Response 201 Created                          │
└──────────────────────────────────────────────────────────────────────────┘
```

### Deterministik Karşılaştırma Algoritması

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    comparisonService.compareAnalyses                     │
│                                                                          │
│   AI KULLANILMIYOR - Tamamen algoritmik karar                           │
│                                                                          │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ STEP 1: Her iki analiz sonucunun issue'larını grupla             │   │
│   │                                                                  │   │
│   │ FROM Analysis Issues:         TO Analysis Issues:                │   │
│   │ ┌────────────────────┐        ┌────────────────────┐             │   │
│   │ │ NESTED_LOOP: high  │        │ NESTED_LOOP: low   │             │   │
│   │ │ MAGIC_NUMBER: low  │        │ UNUSED_VAR: medium │             │   │
│   │ └────────────────────┘        └────────────────────┘             │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ STEP 2: Her issue code için changeType hesapla                   │   │
│   │                                                                  │   │
│   │ ┌──────────────────┬─────────────────────┬────────────────────┐  │   │
│   │ │   DURUM          │   AÇIKLAMA          │   changeType       │  │   │
│   │ ├──────────────────┼─────────────────────┼────────────────────┤  │   │
│   │ │ FROM var, TO yok │ Issue silindi       │ ✅ IMPROVED        │  │   │
│   │ │ FROM yok, TO var │ Yeni issue eklendi  │ ❌ WORSENED        │  │   │
│   │ │ severity ↓       │ Şiddet azaldı       │ ✅ IMPROVED        │  │   │
│   │ │ severity ↑       │ Şiddet arttı        │ ❌ WORSENED        │  │   │
│   │ │ complexity ↓     │ Karmaşıklık azaldı  │ ✅ IMPROVED        │  │   │
│   │ │ complexity ↑     │ Karmaşıklık arttı   │ ❌ WORSENED        │  │   │
│   │ │ Diğer            │ Değişiklik yok      │ ➖ UNCHANGED       │  │   │
│   │ └──────────────────┴─────────────────────┴────────────────────┘  │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ STEP 3: Database'e kaydet                                        │   │
│   │                                                                  │   │
│   │ prisma.comparison.create({                                       │   │
│   │   projectId, fromAnalysisId, toAnalysisId,                       │   │
│   │   results: [                                                     │   │
│   │     { issueCode, changeType, before*, after* }                   │   │
│   │   ]                                                              │   │
│   │ })                                                               │   │
│   └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### Severity & Complexity Ranking

```javascript
SEVERITY_RANK = {
  low: 1, // En iyi
  medium: 2,
  high: 3, // En kötü
};

COMPLEXITY_RANK = {
  O_1: 1, // En iyi (sabit zaman)
  O_n: 2, // Lineer
  O_n2: 3, // En kötü (kuadratik)
};

// Değişim = TO_rank - FROM_rank
// change < 0 → IMPROVED (iyileşti)
// change > 0 → WORSENED (kötüleşti)
// change = 0 → UNCHANGED
```

---

## 💬 AI Açıklama Akışı

Karşılaştırma sonuçları **deterministik** olarak hesaplandıktan sonra, kullanıcı isteğe bağlı olarak AI'dan insan dostu açıklama isteyebilir.

```
┌──────────────────┐
│ POST /api/       │
│ comparisons/     │
│ :id/explain      │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                 comparisonController.generateExplanation                 │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 1. comparisonService.verifyOwnership(id, userId)                   │  │
│  │    → Karşılaştırma kullanıcıya ait mi?                             │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ 2. aiExplanationService.generateExplanation(comparisonId)          │  │
│  │    → Mevcut explanation var mı kontrol et                          │  │
│  │    → Yoksa AI'ı çağır ve oluştur                                   │  │
│  └─────────────────────────────────┬──────────────────────────────────┘  │
│                                    ▼                                     │
│                            Response 201 Created                          │
│                            { success: true, data: explanation }          │
└──────────────────────────────────────────────────────────────────────────┘
```

### AI Açıklama Süreci

```
┌─────────────────────────────────────────────────────────────────────────┐
│                 aiExplanationService.generateExplanation                 │
│                                                                          │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ 1. Mevcut explanation kontrolü                                   │   │
│   │                                                                  │   │
│   │    const existing = await prisma.aIExplanation.findUnique({      │   │
│   │      where: { comparisonId }                                     │   │
│   │    });                                                           │   │
│   │                                                                  │   │
│   │    if (existing) return existing; // Cache hit!                  │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ 2. Gemini AI çağrısı - SADECE AÇIKLAMA İÇİN                      │   │
│   │                                                                  │   │
│   │    Prompt:                                                       │   │
│   │    "Comparison results: ${JSON.stringify(results)}"              │   │
│   │    "Summary: X improvements, Y regressions"                      │   │
│   │    "Write explanation in MARKDOWN..."                            │   │
│   │                                                                  │   │
│   │    ⚠️ AI KARARI DEĞİŞTİRMİYOR                                    │   │
│   │    AI sadece backend'in deterministik kararlarını                │   │
│   │    insan dostuy şekilde açıklıyor                                │   │
│   └──────────────────────────────────────────────────────────────────┘   │
│                                    │                                     │
│                                    ▼                                     │
│   ┌──────────────────────────────────────────────────────────────────┐   │
│   │ 3. Database'e kaydet                                             │   │
│   │                                                                  │   │
│   │    prisma.aIExplanation.create({                                 │   │
│   │      comparisonId,                                               │   │
│   │      explanation: "## Code Comparison Summary..."                │   │
│   │    })                                                            │   │
│   └──────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ Veritabanı İlişkileri

```
┌─────────────┐
│    User     │
│  (Supabase) │
└──────┬──────┘
       │ 1:N
       ▼
┌─────────────┐       1:N      ┌──────────────┐
│   Project   │───────────────▶│  CodeVersion │
└──────┬──────┘                └───────┬──────┘
       │                               │ 1:1
       │                               ▼
       │                       ┌──────────────────┐
       │                       │  AnalysisResult  │
       │                       └────────┬─────────┘
       │                                │ 1:N
       │                                ▼
       │                       ┌──────────────────┐       1:1       ┌─────────────┐
       │                       │  AnalysisIssue   │─────────────────│   Snippet   │
       │                       └──────────────────┘                 └─────────────┘
       │ 1:N
       ▼
┌─────────────────┐
│   Comparison    │
└────────┬────────┘
         │
         ├─────────────────────────────────┐
         │ 1:N                             │ 1:1
         ▼                                 ▼
┌──────────────────┐             ┌─────────────────┐
│ ComparisonResult │             │  AIExplanation  │
└──────────────────┘             └─────────────────┘
```

---

## 🔑 Kritik Tasarım Kararları

### 1. SourceCode Saklanmıyor

```
Kullanıcı Kodu ─────▶ AI Analizi ─────▶ Sonuç Database'de
       │                                       │
       ▼                                       ▼
   SAKLANMIYOR                           Issues, Snippets,
   (Privacy)                             Severity, Complexity
```

**Neden?**

- Kullanıcı kodunun gizliliği
- Database boyutu optimizasyonu
- GDPR/veri minimizasyonu ilkesi

### 2. Deterministik Karşılaştırma

```
AI ──────▶ Analiz (Issue Detection)
           ⚠️ AI burada kullanılıyor

Backend ──▶ Karşılaştırma (Issue Change)
            ✅ Tamamen algoritmik, AI yok

AI ──────▶ Açıklama (Human-Friendly Text)
           ⚠️ AI burada kullanılıyor ama
              KARAR DEĞİŞTİRMİYOR
```

**Neden?**

- Tekrarlanabilirlik (aynı input = aynı output)
- Güvenilirlik (AI hallucination riski yok)
- Audit trail (karşılaştırma mantığı açık ve izlenebilir)

### 3. Lazy AI Explanation

```
Comparison oluştur ─────▶ Results (anında)
                              │
                              ▼
                    Kullanıcı "Açıkla" butonuna tıklarsa
                              │
                              ▼
                  AI Explanation (isteğe bağlı)
```

**Neden?**

- Gereksiz AI çağrısı maliyetinden kaçınma
- Hızlı ilk yanıt (sadece deterministik hesap)
- Kullanıcı tercihi (herkes açıklama istemeyebilir)

---

## 🚨 Hata Yönetimi

Tüm controller'lar aynı pattern'i takip eder:

```typescript
try {
  // İş mantığı
  const result = await service.doSomething();
  res.status(200).json({ success: true, data: result });
} catch (error) {
  console.error("Error description:", error);
  res.status(500).json({ success: false, error: "User-friendly message" });
}
```

### HTTP Status Kodları

| Kod | Kullanım                                      |
| --- | --------------------------------------------- |
| 200 | Başarılı GET                                  |
| 201 | Başarılı POST (yeni kaynak oluşturuldu)       |
| 400 | Validation hatası, eksik parametre            |
| 401 | Token yok veya geçersiz                       |
| 404 | Kaynak bulunamadı veya ownership check failed |
| 500 | Server hatası (database, AI API, vb.)         |

---

## 📊 Özet: İstek Yaşam Döngüsü

```
1. [HTTP Request] ──▶ Express Router ──▶ Route Match
                                              │
2. [Auth Middleware] ◀────────────────────────┘
   - Token decode
   - req.userId set
                │
3. [Controller] ◀───┘
   - Request body/params parse
   - Validation
   - Service çağrısı
                │
4. [Service] ◀──────┘
   - Business logic
   - AI çağrısı (varsa)
   - Prisma DB operations
                │
5. [Database] ◀─────┘
   - CRUD operations
   - Relation handling
                │
6. [Response] ◀─────┘
   - JSON formatting
   - Status code
   - success: true/false
```
